include ../section-header/section-header.pug
include ../list/list.pug

mixin experience(content)
    - const { items } = content;
    section.experience
        .experience__section-header
            +sectionHeader("experience", true)
        section.experience__list
            +list(items)